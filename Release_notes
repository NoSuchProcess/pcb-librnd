librnd 3.1.2
~~~~~~~~~~~~

This release contains only minor bugfixes. Bugfix highlight:
 - proper opengl detection on opengl ES
 - opengl renders thin objects (specifid in pixels) properly; this affects
   the "red" "xor drawn" overlays, e.g. when things are being moved
