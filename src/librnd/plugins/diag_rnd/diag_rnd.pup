$class feature
$short diagnostic acts. for devs
$long Actions for librnd diagnostics, intended for developers. These are not in core because end users normally don't need these. As a plugin, due to dynamic loading, it can be dropped on an existing installation with minimal risk of scaring away a reproducible bug.
$package (core)
$state works
default buildin
autoload 1
